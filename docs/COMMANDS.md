# 🚀 Команди проєкту (Makefile)

Цей проєкт використовує **Makefile** для автоматизації роботи. Замість того, щоб вводити довгі команди в терміналі, ви просто пишете `make` та назву дії.

### 🛠 Docker: Керування серверами

Команди для запуску та зупинки вашого середовища розробки.

| Команда           | Що вона робить (простою мовою)                                                                                    |
| :---------------- | :---------------------------------------------------------------------------------------------------------------- |
| `make up`         | Просто запускає проєкт. Термінал залишається вільним для роботи.                                                  |
| `make dev`        | Запускає проєкт і показує все, що відбувається (логи) в реальному часі.                                           |
| `make build`      | Повністю перезбирає проєкт (використовуйте, якщо змінили Dockerfile або скачали нові бібліотеки до package.json). |
| `make build-prod` | Перевірка готовності. Запускає компіляцію коду (TypeScript) та перевіряє типи для випуску в продакшн.             |
| `make down`       | Зупиняє та вимикає всі запущені сервіси проєкту.                                                                  |
| `make restart`    | Перезапускає всі сервіси(контейнери).                                                                             |
| `make logs`       | Показує історію логи від усіх частин проєкту.                                                                     |
| `make logs-api`   | Показує логи тільки від бекенду (NestJS).                                                                         |
| `make logs-web`   | Показує логи тільки від фронтенду (Next.js).                                                                      |

---

### 🚀 Деплой та Хмара: AWS Copilot ☁️

Команди для керування вашим проєктом у реальному світі (на серверах Amazon).

| Команда                | Що вона робить (простою мовою)                                                                        |
| :--------------------- | :---------------------------------------------------------------------------------------------------- |
| `make deploy-web`      | Відправляє сайт в інтернет. Збирає Docker-образ та оновлює ваш сайт на серверах AWS.                  |
| `make logs-prod-web`   | Показує логи прямо з сервера (production). Допомагає зрозуміти, чому сайт впав або працює не так.     |
| `make status-prod-web` | Показує "здоров'я" вашого сервера: скільки пам'яті споживається та за яким посиланням доступний сайт. |

---

### 🧪 Тестування: Jest

Перевірка стабільності вашого коду та роботи компонентів.

| Команда              | Що вона робить (простою мовою)                                                 |
| :------------------- | :----------------------------------------------------------------------------- |
| `make test`          | Запускає **всі** тести в усьому проєкті.                                       |
| `make test-affected` | Запускає тести тільки для тих частин, які ви **змінили** (працює дуже швидко). |

---

### 📂 База даних: Prisma & Nx

Команди для налаштування таблиць та роботи з даними.

| Команда            | Що вона робить (простою мовою)                                       |
| :----------------- | :------------------------------------------------------------------- |
| `make db-generate` | Оновлює "зв'язок" коду з базою (треба запускати після змін у схемі). |
| `make db-migrate`  | Зберігає ваші зміни в структурі бази (наприклад, нова таблиця).      |
| `make db-studio`   | Відкриває зручну сторінку в браузері для редагування даних у базі.   |

---

### 🧹 Якість коду та обслуговування

Інструменти для підтримки чистоти та виправлення помилок у монорепозиторії.

| Команда       | Що вона робить (простою мовою)                                      |
| :------------ | :------------------------------------------------------------------ |
| `make lint`   | Перевіряє код на наявність дрібних помилок або "брудного" стилю.    |
| `make format` | Автоматично виправляє відступи, пробіли та коми (Prettier).         |
| `make sync`   | Лагодить зв'язки між проєктами (якщо бачите помилку "out of sync"). |
| `make clean`  | "Очистити все" — видаляє кеш, дані бази та папку зі збіркою.        |
