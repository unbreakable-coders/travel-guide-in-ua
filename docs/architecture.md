# 🏛 Project Architecture

Цей документ описує архітектуру фронтенд-частини проєкту, принципи організації коду та правила залежностей.  
Мета - зробити код **читабельним, масштабованим і однаковим для всієї команди**.

---

### 🌟 1. Загальні принципи

- Проєкт побудований на **Vue 3 + Vite**
- UI базується на **Vuetify**
- Архітектура орієнтована на **feature-based підхід**
- Мінімізуємо глобальні залежності
- UI, бізнес-логіка та робота з API **розділені**

---

### 🗂 2. High-level структура

```text
src/
  app/            # bootstrap та ініціалізація
  pages/          # сторінки (route-level)
  features/       # функціональні можливості
  entities/       # доменні сутності
  shared/         # спільні ресурси
```

---

### 🏗 3. Шари та їх відповідальність

### `app/`

- **main.ts** — точка входу
- Підключає:
  - Vue Router
  - Vuetify
  - Глобальні providers
  - Ініціалізацію Supabase клієнта

- ❗ У `app/` **немає бізнес-логіки**

---

### `pages/`

- Відповідають за маршрути
- Складають UI з `features` та `entities`
- **Не містять складної логіки**

**Приклади:**

```
pages/
  HomePage/
  PlacePage/
  ProfilePage/
```

---

### `features/`

- Конкретні **user-actions / сценарії**
- Можуть містити:
  - компоненти
  - composables
  - роботу з API

**Приклади:**

```
auth/
favorites/
search/
filters/
```

---

### `entities/`

- Доменні сутності (бізнес-обʼєкти, не UI)
- Містять:
  - типи
  - базові компоненти (PlaceCard тощо)
  - запити до Supabase для цієї сутності

**Приклади:**

```
place/
user/
category/
review/
```

---

### `shared/`

- Загальні ресурси, **не належать конкретній фічі**

**Структура:**

```
shared/
  ui/        # базові UI-компоненти
  api/       # supabase client, helpers
  config/    # константи, env
  utils/     # хелпери
  assets/    # іконки, картинки
```

---

### 🎨 4. Vuetify layer

- Ініціалізація централізована
- Theme, icons, defaults — в одному місці
- ❌ Не використовуємо inline-override стилі без потреби

**Рекомендовано:**

```
app/plugins/vuetify.ts
```

- Кастомні компоненти — у `shared/ui`

---

### 🚦 5. Routing

- Використовується **Vue Router**
- Роут = сторінка з `pages/`
- Guards та meta описуються централізовано
- Детально в [routing.md](routing.md)

---

### 💾 6. Supabase integration

- Supabase клієнт створюється **один раз**
- Всі запити інкапсульовані в `shared/api` або `entities`
- Компоненти **не працюють напряму з Supabase**
- Детально в [supabase.md](supabase.md)

---

### 🔗 7. Dependency rules

- **Дозволені залежності:**
  - `pages` → `features`, `entities`, `shared`
  - `features` → `entities`, `shared`
  - `entities` → `shared`
  - `shared` → ❌ ні від кого

- ❌ Заборонено імпортувати `pages` або `features` всередині `shared`

---

### 🏷 8. Naming conventions

- Папки — **kebab-case**
- Компоненти — **PascalCase.vue**
- Composables — **useSomething.ts**
- Типи — **SomethingType** або **Something**
