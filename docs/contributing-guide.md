# 🤝 Contributing Guide

Цей документ описує правила командної роботи над проєктом **Travel Guide Platform**.  
Мета - забезпечити стабільну розробку, зрозумілий Git-процес та єдині підходи до коду.

---

### 🌿 1. Branching strategy

У проєкті використовуються **дві основні гілки**:

### `main`

- стабільна, **production-ready** версія
- оновлюється **тільки** з `develop`
- **захищена** від:
  - прямих комітів
  - видалення
- merge — **лише через Pull Request**

### `develop`

- основна гілка для розробки
- усі фічі, фікси та технічні зміни **мерджаться сюди**
- **захищена** від:
  - прямих комітів
  - видалення
- merge — **лише через Pull Request**
- мінімум **1 review approval** обовʼязковий

---

### ⚡ 2. Feature workflow

1. Створюємо гілку **від `develop`**
2. Працюємо у своїй гілці
3. Відкриваємо Pull Request **в `develop`**
4. Проходимо **code review**
5. Після approval — merge у `develop`

---

### 🏷 3. Branch naming

Формат гілок:

- `feature/<short-description>`
- `fix/<short-description>`
- `chore/<short-description>`

**Приклади:**

```text
feature/org-panel-calendar
feature/user-bookings-page
fix/booking-status-bug
chore/update-eslint-config
```

---

### 💾 4. Commits

Коміти мають бути короткими та зрозумілими.
Рекомендований формат:

```
type: короткий опис
```

**Типи:**

- `feat` — новий функціонал
- `fix` — виправлення багів
- `refactor` — рефакторинг без зміни логіки
- `chore` — технічні зміни
- `docs` — документація

**Приклади:**

```
feat: add organization calendar
fix: correct booking status logic
docs: update routing documentation
```

---

### 🔀 5. Pull Requests

PR обовʼязковий для будь-якого merge у:

- `develop`
- `main`

**Вимоги до PR:**

- мінімум 1 approval
- одна задача = один PR
- код збирається (`npm run build`)
- немає ESLint помилок
- зрозумілий опис

---

### 🔄 6. Merge flow

- `feature/*` → `develop`
- `fix/*` → `develop`
- `develop` → `main` (реліз / стабільний етап)

❗ Прямий merge або push у `main` **заборонений**

---

### 🖌 7. Code style & structure

- Дотримуємось **ESLint + Prettier**
- Бізнес-логіка не живе у компонентах сторінок
- Робота з даними — через `shared/api` або `entities`
- Vuetify використовуємо системно, без копіювання стилів
- Не додаємо `any` без обґрунтування

---

### 🔑 8. Environment variables

- `.env.local` **ніколи не комітимо**
- ключі Supabase поширюються приватно
- зміни env — тільки після узгодження з командою

---

### ✅ 9. Definition of Done (DoD)

Задача вважається завершеною, якщо:

- реалізований потрібний функціонал
- немає lint-помилок
- проєкт збирається
- PR має мінімум 1 approval
- документація оновлена (якщо потрібно)

---

### 💬 10. Communication & decisions

- Сумнівні зміни обговорюємо до реалізації
- Краще поставити питання, ніж ламати архітектуру

---

### 🙌 11. Respect

- **Повага до команди!!!**
- Критикуємо **код**, не людей 😊
