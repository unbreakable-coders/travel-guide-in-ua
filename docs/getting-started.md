# 🚀 Як усе запустити?

Це короткий гайд про те, як розгорнути проект у себе на компі та що взагалі з ним робити.

### 🛠 Швидкий старт

Щоб просто зараз підняти локальний сервер і почати кодити, пиши в терміналі:

```bash
npm run dev
```

---

### 📜 Наші скрипти

Ось список команд, які тобі знадобляться найчастіше:

### Коддинг та розробка

- **`npm run dev`** — запускає локальну розробку.

### Збірка та перевірка (Build / Preview)

Якщо треба підготувати проект до релізу або глянути, як він працюватиме "по-справжньому":

- **`npm run build`** — збирає проект у продакшн-версію.
- **`npm run preview`** — запускає вже зібраний білд локально. Корисно, щоб перевірити, чи нічого не відвалилося після збірки.

### Чистота та краса

- **`npm run lint`** — перевірка коду через ESLint. Шукає помилки та підказує, де ми натупили.
- **`npm run format`** — автоматичне форматування через Prettier. Робить код гарним і за замовчуванням однаковим для всіх.

---

### 💡 Як ми зазвичай працюємо

1. Пишемо код на `npm run dev`.
2. Наводимо марафет через `npm run format`.
3. Перед тим, як кидати ПР (Pull Request), про всяк випадок робимо `npm run build`, щоб переконатися, що все збирається без помилок.
